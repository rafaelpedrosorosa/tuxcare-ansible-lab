---
- name: Instalar e registrar o TuxCare KernelCare na VM local
  hosts: kernelcare
  become: true
  gather_facts: true
  roles:
    - role: tuxcare_kernelcare
      vars:
        tuxcare_activation_key: "{{ vault_tuxcare_activation_key }}"
